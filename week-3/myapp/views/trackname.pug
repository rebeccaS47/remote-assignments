extends layout.pug

block content
    form(action=`/trackName?name=${name}`, method='get')
        label Please enter your name: 
        input(type='text' name='name')
        button(type='submit') Submit